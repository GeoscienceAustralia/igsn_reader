CREATE TABLE OAIPMH 
(
	OAIPMH_ID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL UNIQUE,
	OAIPMH_KEY TEXT NOT NULL UNIQUE,
	OAIPMH_URL TEXT NOT NULL UNIQUE
);

CREATE TABLE SAMPLE 
(
	SAMPLE_ID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL UNIQUE,
	IDENTIFIER TEXT NOT NULL UNIQUE,
	DATESTAMP DATETIME,
	ALT_IDENTIFIERS TEXT,
	TITLE TEXT,
	SUBJECT TEXT,
	DESCRIPTION TEXT,
	TYPE TEXT,
	FORMAT TEXT,
	COVERAGE TEXT,
	CREATOR	TEXT,
	PUBLISHER TEXT,
	RIGHTS TEXT
);